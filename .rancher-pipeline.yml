stages:
  - name: Build image
    steps:
      - publishImageConfig:
          dockerfilePath: ./Dockerfile
          buildContext: .
          tag: minvar/sample-api
          pushRemote: true
          registry: 	docker-registry

  - name: Deploy
    steps:
      - applyYamlConfig:
          path: ./kubernetes.yaml
